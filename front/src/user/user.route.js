const express = require("express");
const router = express.Router();
const controller = require("./user.controller.js");

// router.get("/login", controller.getLoginPageData);

module.exports = router;
